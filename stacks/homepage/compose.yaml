services:
  homepage:
    image: ghcr.io/gethomepage/homepage:v1.8.0
    volumes:
      - ./config:/app/config
    networks:
      caddy-proxy:
        aliases:
          - homepage

networks:
  caddy-proxy:
    external: true
