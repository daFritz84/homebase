services:
  homepage:
    image: ghcr.io/gethomepage/homepage:v1.8.0
    volumes:
      - ./config:/app/config
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      caddy-proxy:
        aliases:
          - homepage

networks:
  caddy-proxy:
    external: true
