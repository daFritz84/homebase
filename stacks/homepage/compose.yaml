services:
  homepage:
    image: ghcr.io/gethomepage/homepage:v1.8.0
    environment:
      HOMEPAGE_ALLOWED_HOSTS: homepage.home.arpa
    volumes:
      - ./config:/app/config
    networks:
      caddy-proxy:
        aliases:
          - homepage

networks:
  caddy-proxy:
    external: true
